Pill for amenities and filters — the one place the brand uses full-round geometry.

```jsx
<Tag icon="wifi">Wi-Fi</Tag>
<Tag onSelect={() => {}} selected>Kajak</Tag>
<Tag variant="filled" onRemove={() => {}}>2 fő</Tag>
```
