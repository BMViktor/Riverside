Square icon-only control for toolbars, gallery arrows and close buttons.

```jsx
<IconButton icon="x" label="Bezárás" />
<IconButton icon="chevron-right" variant="inverse" round label="Következő kép" />
```

`inverse` adds a blur backdrop so it survives on top of photos.
