Intentional addition: booking is the brand's core action, so the arrival/departure pair is a primitive rather than kit-local markup.

```jsx
<DateRangeField fromProps={{ defaultValue: '2026-07-10' }} toProps={{ defaultValue: '2026-07-13' }} />
```
