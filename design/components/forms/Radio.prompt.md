Single-choice control; `RadioGroup` handles layout.

```jsx
<RadioGroup>
  <Radio name="fizetes" label="Előre utalás" description="Visszaigazolás után 3 napon belül." defaultChecked />
  <Radio name="fizetes" label="Fizetés érkezéskor" />
</RadioGroup>
```
